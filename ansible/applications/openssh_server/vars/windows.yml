# =========================================================================================== #
# File: '{{ role_path }}/vars/windows.yml'
# --- [ Description ] ----------------------------------------------------------------------- #
#
# Windows-family overlay merged over openssh_server_defaults by the v3 loader.
#
# The OpenSSH server is delivered as a Windows optional capability, so the defaults are already
# the Windows values and this overlay carries nothing. It exists because the loader resolves an
# OS overlay per family; an empty map is the honest statement that this family needs no override,
# rather than a duplicate of defaults that would drift.
#
# =========================================================================================== #
---
{}
