# =========================================================================================== #
# File: '{{ role_path }}/vars/windows.yml'
# --- [ Description ] ----------------------------------------------------------------------- #
#
# Windows-family overlay merged over workspace_network_baseline_defaults by the v3 loader.
#
# The Windows firewall is a Windows-only surface, so the defaults are already the Windows
# values and this overlay carries nothing. It exists because the loader resolves an OS overlay
# per family; an empty map is the honest statement that this family needs no override.
#
# =========================================================================================== #
---
{}
